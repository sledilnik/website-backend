---
ingressroute:
  enabled: true
  rule: Host(`backend.sledilnik.org`)
  certResolver: letsencrypttest

configmap:
  data:
    DJANGO_SETTINGS_MODULE: sledilnik.settings.kube
    SECRET_KEY: changeme